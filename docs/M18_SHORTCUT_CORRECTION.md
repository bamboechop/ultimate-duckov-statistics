# Native shortcuts while UDS is open

The user confirmed F8 opening/reopening, Ctrl+Tab and Ctrl+Shift+Tab navigation, Escape canceling hotkey capture, and Escape closing the panel with movement restored. Plain or modified Tab nevertheless opened Duckov inventory above UDS. The original report and installed source are preserved in [UI qualification](M18_UI_QUALIFICATION.json).

Installed Duckov 2.3.30 directly dispatches `UI_Inventory` to `CharacterInputControl.OnUIInventoryInput`. This method does not consult `InputManager.InputActived`; UDS's existing owned movement blocker therefore cannot stop it. `OnUIMapInput` and `OnUIQuestViewInput` have the same bypass. `OnReloadInput` also reaches `ItemAgent_Gun.BeginReload` without checking that blocker. The independently inspected core DLL has SHA-256 `298d5d5885427632d5a94b2f3ce587f8ebc9528ec71e575a475158c326ecae8f`.

Four exact callback prefixes suppress these native actions while the panel owns input, including its hotkey and reset confirmation modals. Native configured bindings still determine which physical keys invoke the callbacks. UDS does not disable shared InputActions or replace native View ownership. Existing movement blocking, pointer shielding and Escape handling remain in place. Close, failed shell construction, unusable-shell cleanup and disposal release the guard. Prefixes install once per panel lifecycle under a dedicated Harmony owner; they are passive while closed. Owned cleanup preserves foreign patches, and failed unpatching retains the existing retry mechanism.

Installation validates exact public instance `void(InputAction.CallbackContext)` methods and patch ownership. Open-panel ticks inspect four shared patch-state tokens without re-reading patch metadata. Missing Harmony, incompatible contracts or changed patch state leave the UI accessible, mark this shortcut isolation unavailable under Menu access, and issue a bounded warning. This is a UI runtime state and does not change persisted statistics or exported capability history.

Eight added shell cases exercise actual panel/guard composition through explicit native and Harmony boundaries: four callback dispatch cases, modal cancellation, failed construction/destroyed shell, incompatible patch ownership, and removed patches/failed cleanup/re-activation. Existing Harmony reflection boundary code is shared between test projects without changing its behavior. These managed tests do not execute the game's actual Input System or Unity rendering.

Native repeat remains required: at base, open UDS with F8; press Tab, Ctrl+Tab and Ctrl+Shift+Tab; then the configured map, quest and reload bindings. Native views/reload must remain inactive while UDS navigation works. Cancel hotkey capture with Escape, then close UDS with Escape and verify the normal native shortcuts and movement return. This scoped correction does not establish the broader deferred gameplay/controller input-isolation qualification, measured UI resource cleanup, or native performance acceptance.

Source `c2e88861d28e0a0b5adc03f8a5fbbc5289feabf7` passed 1,908 main and 28 shell tests in each Debug and Release, including both game-closed deployment tests. Native builds had zero warnings/errors; changed-source formatting/analyzers, installed contracts, source/artifact audits and independent full correction review passed. The two isolated roots and ordinary checkout produced identical DLL/PDB/ZIP bytes. The 631,010-byte ZIP was independently extracted and verified. Deployment at `2026-09-08T17:09:59.9910104Z` verified all five installed files and a prior-package rollback, preserving all 2,051 UDS data files byte-for-byte. [Delivery identities](M18_SHORTCUT_CORRECTION.json) include the new unmeasured campaign; native repeat and remaining M18 gates stay open.
