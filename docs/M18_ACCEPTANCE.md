# M18 acceptance record

M18 hardens the completed M17 feature baseline. Starting source: `443fad6a268916a6038c919501a714d1f17da371`. Publication is user-controlled. **Native qualification remains open; M18 is not complete and the candidate is not declared production-ready.** Live delivery state belongs on [GitHub](https://github.com/bamboechop/ultimate-duckov-statistics).

| Requirement | Implementation and evidence | Status |
| --- | --- | --- |
| Frozen source/native baseline | Clean starting checkout; installed Duckov 2.3.30 / Steam 24013657 / Unity 2022.3.62f2 / Harmony 2.4.1.0 probe | Pass |
| Current v1 persistence | `uds-profile-v1`, separate `v1` namespace, predecessor readers/fields removed; current semantic validation, atomic recovery, generation/reset/export and pending retry retained | Pass for automated composition, observed completed-run cold reopen and same-package reinstallation with native reopen; native recovery remains open |
| Supported native metrics and removal | [Removal inventory](M18_REMOVAL_INVENTORY.md); impossible firing/tote/crafting/Cash surfaces and exclusive consumers removed | Pass (source/native contract review) |
| UI runtime cleanup | Immediate renderer, appearance rejection, unused formatters/production fixture constants removed; hidden Diagnostics measurement fixed. Eleven real-panel/shell regressions through isolated boundaries, including actual menu injection and ancestor-canvas resolution | Pass (isolated); native tab/scroll/detail/tooltip acceptance passes below; remaining settings/input checks and measured resources remain open |
| Native shortcut isolation | [User report and passed controls](M18_UI_QUALIFICATION.json); [four-callback correction and boundary evidence](M18_SHORTCUT_CORRECTION.md) | Reviewed correction deployed; 1,908 main + 28 shell tests in each configuration, reproducibility and exact installed hashes pass; corrected native repeat remains open |
| Runtime cost/durability | Terminal attribution freeze and bounded terminal/transition retries; live integrity enumeration avoids a per-event list; 144,000 mixed operations with 0/200 prior runs, exact six-segment/three-map persistence/reopen | Pass (managed); native performance Not exercised |
| Performance protocol | [Frozen 14-cell matrix](M18_CAPTURE_MATRIX.json), three B/D repetitions, existing engineering/spike/ceiling rules; campaign source/DLL/raw-data checks | Pass (prepared/tested); native captures Not exercised |
| Artifact privacy/reproducibility | Normalized PE/PDB identities, canonical LF source text, UTF-8/UTF-16/package audits and ordinary IL diagnostic-call rejection. Both isolated roots and the Windows checkout produce identical DLL/PDB/ZIP hashes at the source below | Pass |
| Complete automated qualification | 1,900 main + 11 shell tests in **each** Debug and Release, including the two game-closed deployment tests. Changed-source formatting/analyzers, installed probe, zero-warning native Debug/Release builds, artifact/source safety and independent correction reviews pass. Live [CI](https://github.com/bamboechop/ultimate-duckov-statistics/actions?query=branch%3Acodex%2Fm18-release-hardening) | Pass (local) |
| Independent whole-system review | Native/UI, persistence/economy/world-time/crafting and delivery reviews; correction re-reviews below; final complete immutable range `443fad6..4068480` | Pass; no confirmed P1/P2 |
| Package/deployment | 627,022-byte deterministic five-file ZIP; independent extraction; exact installed-file readback while Duckov closed; verified prior-candidate rollback copy; all 1,903 UDS data files unchanged (647 v1, 1,256 older/other); no deployment residue | Pass |
| Fresh-save baseline | Save 3 reached base; [baseline export evidence](M18_SLOT3_BASELINE.json) preserves all 37 export files by checksum. One Kartoffel, one Verband, 55 Cash inflow/held and zero runs agree with the user's report. Native diagnostics observed the base pause-menu entry activation | Pass for this baseline and entry callback only |
| Native tutorial run | A subsequent fresh generation on save 3 records one extraction across both guide segments. Screenshot, complete saved run, JSON and targeted CSV breakdowns agree: 168.7574764 active seconds, 625.020558 m, 4 kills, 9 containers and 58 Cash in Raid context. [Qualification evidence](M18_TUTORIAL_NATIVE_QUALIFICATION.json) preserves 37 export files and the supplied screenshot | Pass for observed tutorial route/extraction, data agreement and completed-run cold reopen; native death/retry remains open |
| Native direct quit | [Reopen evidence](M18_TUTORIAL_REOPEN_QUALIFICATION.json): unchanged completed tutorial run, but false lifetime Cash outflow with holdings still 58. [Correction](M18_DIRECT_QUIT_CASH_CORRECTION.json) suspends Cash scans before native inventory destruction and preserves pending flows | Pass on corrected source for [observed direct quit/restart](M18_DIRECT_QUIT_NATIVE_QUALIFICATION.json); historical failure preserved |
| Native completed sleep | [Base-action evidence](M18_BASE_ACTIONS_QUALIFICATION.json): one user-reported 1h 40m sleep; saved profile, JSON and CSV record one completion and exactly 6,000 advanced game seconds. Completed run and non-world-time statistics remain unchanged | Pass for observed completed sleep and persisted/exported data agreement |
| Native regular raid and healing | [Regular-raid evidence](M18_REGULAR_RAID_QUALIFICATION.json): one Nullpunkt extraction with native raid ID 1, 3 kills (2 ranged, 1 melee), two Verband restoring 10 HP, one Limonade and two containers (starter crate plus medical box). User corrected the initially omitted starter crate; saved run/JSON/CSV agree and the tutorial remains unchanged | Pass for this single-map extraction, reported actions and cold reopen; multi-map/revisit and death pass separately below |
| Native save isolation and durable run/sleep totals | [Save-switch evidence](M18_SAVE_ISOLATION_QUALIFICATION.json): full restart, slot 1 export and return to slot 3. Separate generations; slot 1 has no runs/sleep/item uses or recorded currency flows. Slot 3 retains both runs, one 100-minute sleep, 10 HP restored and 136 Cash held, with no additional outflow | Pass for observed slot 1/3 isolation and current-format cold reopen |
| Native paid crafting | [Paid-craft evidence](M18_PAID_CRAFT_QUALIFICATION.json): slot 1 crafts one Tasse from four Polyethylen-Folie for 150; Money falls from 13,374 to 13,224. Saved profile/JSON/CSV agree on output, recipe, resources and charge; Economy records the exact Money outflow in Base context with its documented UnknownAdjustment source | Pass for this successful paid craft, data agreement and cold reopen |
| Native shop sale | [Sale evidence](M18_SHOP_SALE_QUALIFICATION.json): the user-reported one MF sale on slot 1 adds 1,284 Money, attributed to Sale/Shop; balance 13,224 to 14,508. Saved profile/JSON/CSV agree on currency metrics and holdings, with crafting unchanged | Pass for this Money sale, data agreement and cold reopen |
| Same-package reinstallation | [Reinstallation evidence](M18_REINSTALLATION_QUALIFICATION.json): exact five-file candidate replaced while Duckov was closed; verified rollback, no transaction residue, and all 1,718 UDS data files unchanged (462 v1, 1,256 older/other). Native slot 1 reopen retains its generation, craft, sale and 14,508 Money; slot 3 byte preservation retains both runs, sleep and currency totals | Pass for replacement, byte preservation and observed slot 1 native reopen |
| Native multi-map/revisit and weapon switch | [Multi-map evidence](M18_MULTIMAP_QUALIFICATION.json): one slot 1 extraction across Lagerbereich, Keller and Lagerbereich. Four MMG kills on the first visit (two wolves/two ducks), zero in Keller, two SR-3M duck kills on return. Saved run/JSON/CSV and the supplied 2560x1440 Runs screenshot agree, including route values and equipment slot/attachment states; corpse accesses add no containers | Pass for this route, per-visit direct-fire attribution, data agreement, observed Runs visual state and subsequent [cold reopen](M18_BURNING_REOPEN_QUALIFICATION.json); broader UI checks remain open |
| Native death and killer attribution | [Death evidence](M18_DEATH_QUALIFICATION.json): one Nullpunkt Died run with one Schrott-Bogen duck kill and one player death attributed to Spider-Bot. Saved run/JSON/CSV agree, terminal loadout is captured before teardown, and the prior multi-map run is unchanged. The user confirms the separate short extraction with one Kriegsaxt kill | Pass for ordinary raid death, bow kill, killer/terminal capture, data agreement and subsequent [cold reopen](M18_BURNING_REOPEN_QUALIFICATION.json); tutorial death/retry and death UI remain separate |
| Native grenade after weapon switch and trap use | [Grenade evidence](M18_GRENADE_QUALIFICATION.json): one Granate use records 60 damage and two duck kills after the user switches to SR-3M; no rifle firing or ranged kill credit. One confirmed Tierfalle use records 10 damage and no kills. Saved run/JSON/CSV agree and all three prior runs remain unchanged. Equipment separately records SR-3M selected at impact | Pass for these uses, grenade damage/kill identity and data agreement; grenade launch-time equipment and delayed-buff origin are not established |
| Native burning after weapon switches | [Burning evidence](M18_BURNING_QUALIFICATION.json): one Brand-Muni use and a user-observed burn kill after bow/SR-3M switches. Source `f0187b2` records 6 Environmental damage, 29 Unknown periodic damage and one Unknown world death, with zero player credit; saved run/JSON/CSV agree and all four prior runs are unchanged. [Correction evidence](M18_BURNING_CORRECTION.json) records exact grenade-to-clone provenance, 16 regressions, independent immutable review, reproducible package and game-closed deployment | Historical failure preserved; automated correction and subsequent native damage/switch/kill qualification below pass on `e4824ff` |
| Burning-correction startup and five-run cold reopen | [Reopen evidence](M18_BURNING_REOPEN_QUALIFICATION.json): slot 1 reopens on `e4824ff` with all 11 combat hooks installed and the new grenade-hazard capability Supported. All five run statistics, crafting, sleep and Economy totals are retained; Money/Cash remain 13,954/60. Current saved runs equal JSON, and 33 export files remain byte-identical to the previous export | Pass for startup, hook installation and persisted-data agreement; live burning damage is qualified separately below |
| Corrected native burning damage | [Repeat evidence](M18_BURNING_REPEAT_QUALIFICATION.json): one Brand-Muni use records 21 damage credited to Player, Brand-Muni and DamageOverTime, with zero kills consistent with the surviving duck. Saved run/JSON/CSV agree, and the preceding five runs are unchanged. The user confirms no weapon switches in this attempt. The intervening farming run and crafting remain outside this action tally | Pass for this burning damage and live origin correlation; switches and burning final blows are qualified separately below |
| Corrected burning kill through held-weapon switches | [Switch/kill evidence](M18_BURNING_SWITCH_KILL_QUALIFICATION.json): one Brand-Muni use records 35 player damage and one Effect kill under Brand-Muni, with zero firing/melee actions. Six alternating SR-3M/bow selections fall within the recorded combat window; the damage/kill equipment association retains the original throw state. Saved run/JSON/CSV agree and all seven prior runs are unchanged. The user confirms the latest Runs view shows 35 damage/1 kill and the Combat lifetime Brand-Muni row shows 56 damage/1 kill | Pass for this observed same-map damage, burn final blow, held-weapon switches, originating equipment retention and targeted UI/data agreement; broader UI and other M18 gates remain open |
| Native tab, scrolling, detail and tooltip check | [UI evidence](M18_UI_QUALIFICATION.json): the user reports Looking good after being asked to visit all nine tabs, scroll long pages, expand Combat weapon details, hover Equipment attachments and check for missing/clipped content, stuck scrolling or broken tooltips. Text polish remains deferred | Pass by user confirmation for the requested checks in the current setup; 720x480 layout also passes by user report and screenshot; the English switch exposed stale entity names, now corrected and confirmed working in the subsequent native repeat. Remaining input checks and measured native resources remain open |
| Current-language entity names | [Correction and delivery](M18_NATIVE_NAME_CORRECTION.json): exact native metadata at presentation, language-event/reopen refresh and preserved recorded fallback. Seventeen new regressions, independent complete correction review, reproducibility and five-file game-closed deployment pass; all 2,014 UDS data files unchanged | Pass for user-confirmed English/German switching and [cold-reopen/export retention](M18_NATIVE_NAME_QUALIFICATION.json): all nine pre-deployment runs and recorded statistics/names retained |
| Remaining native user gates | [Manual sequence](M18_MANUAL_QUALIFICATION.md): progressed-history soak, remaining gameplay scenarios, degradation, recovery, complete UI/export agreement and performance | Not exercised |

## Confirmed corrections and independent re-review

- `c24275da06f81e956aa20cf75acc529607552936`: terminal checkpoint failure freezes original raid attribution and retries with bounded backoff; actual lifecycle/coordinator/economy regression and independent correction review passed.
- `41f16874e9c5e0eea94dc245f5a398f90cd0ca51`: delayed effects cannot capture a later loadout as their origin. Missing origin remains unknown, with independently proven outcome retained. Correction review passed.
- `34f1b89` and `a419c756946c6727ab7801c4408b0637623563df`: profile-transition retry/backoff and shared injected clock; exact step/generation and queued-flow tests and correction review passed.
- `6f352290c5f25a2e18b927447dd2fd5aff282bb1`: staging/backup folders moved outside `Mods` after installed loader review showed dot-prefixed folders are still discoverable. Denied-cleanup regression and delivery re-review passed. The same commit passed byte-identical two-root DLL/PDB/ZIP comparison.
- `eaf083bedff494d6800b142ff39f6eee6361875f`: real-shell overflow/access, 25 open/close cycles and 10,000 closed ticks through isolated boundaries passed.
- `9a99b02d266efb39b81b9a24b8094b89584ad58c`: live integrity optimization, exact native dictionary contract and 144,000-event repository/tracker workload. Independent complete correction-range review passed with no confirmed P1/P2. Managed timing must be collected alone.
- `e4824ff9d34d87b9e6b2b9a5c09e67ae3df39ceb`: exact created grenade-zone and buff correlations preserve observed thrower/item origins through weapon changes. Sixteen regression cases and independent complete immutable review `24e5398..e4824ff` pass. Native fields remain unchanged; subsequent native burning damage, kill, held-weapon-switch retention and targeted Runs/Combat values pass as recorded above.

These reviews inspected production/native composition, including unchanged supported code. The final integration, current-data/runtime and delivery reviewers independently passed the complete or assigned bounded immutable range ending at `4068480cd736642a16e14b0510384c957b3b08e0`. Native user qualification remains separate.

## Final identities and outstanding gates

Current deployed binary/package source: [`c2e88861d28e0a0b5adc03f8a5fbbc5289feabf7`](https://github.com/bamboechop/ultimate-duckov-statistics/commit/c2e88861d28e0a0b5adc03f8a5fbbc5289feabf7), version `1.0.0-rc.1`. This adds owned inventory/map/quest/reload shortcut isolation to the previously qualified corrections. Later evidence/documentation commits do not change this binary identity. [Shortcut correction evidence](M18_SHORTCUT_CORRECTION.json) records tests, independent immutable review, package/reproducibility and deployment. [Name correction evidence](M18_NATIVE_NAME_CORRECTION.json), [burning correction evidence](M18_BURNING_CORRECTION.json) and earlier records preserve prior deliveries and their native acceptance. Core DLL bytes are unchanged from those deployments; they still differ from the historical managed-workload source `4068480`. No new native performance acceptance is claimed.

| Artifact | SHA-256 |
| --- | --- |
| `UltimateDuckovStatistics-v1.0.0-rc.1.zip` (631,010 bytes) | `7e9ae7bcd2febbec9e9b6de31263053cd49041f7da287c62f5beda0ae7bd021f` |
| `UltimateDuckovStatistics.dll` | `110645e17a63959fbab2e48f88e62230bfc824b613f2bf1b3ce090984b3d8787` |
| `UltimateDuckovStatistics.Core.dll` | `293ff0158ec6d3b80eef46a952ecd6bf8b1157f791a7cd539f4a418a8c1a2549` |

Local candidate: `artifacts/candidates/c2e88861d28e0a0b5adc03f8a5fbbc5289feabf7/UltimateDuckovStatistics-v1.0.0-rc.1.zip`. Frozen campaign: `artifacts/qualification/c2e88861d28e0a0b5adc03f8a5fbbc5289feabf7/campaign.json`, SHA-256 `377cf13bcb5a51341d9dc5c2ec2dfdfaed2cabf491a3719ee05cb99c084d0f97`. Fill its adjacent `controls.json` from the actual user-selected setup; no native performance captures have been collected. Verified rollback package: `artifacts/deployment-backups/eb419fefb5ea480c96733f0be6d3eadc/UltimateDuckovStatistics`. All 2,051 UDS data files were unchanged across the shortcut-correction deployment; Duckov saves were not modified.

The final isolated .NET 8 workload retained exact totals after **144,000 operations per case**, six segments and three maps, with zero and 200 prior runs. Median processing per 6,000 operations was 50.003 / 51.049 ms; the early/middle/late medians were 50.556/50.880/49.323 ms and 130.932/49.409/49.495 ms. These runs show no progressive processing slowdown within this workload. Each synthetic operation allocated approximately 12,752 bytes, including event construction and managed fan-out. Whole-test-process retained heap changed from 21,851,336 to 22,228,720 bytes and from 22,908,136 to 23,727,472 bytes. These are diagnostic observations, not native frame-time or memory acceptance.

Checkpoint plus complete-profile snapshot writes were measured separately: median 16.497 / 113.981 ms, maximum **326.330 / 213.901 ms**, at final profile sizes 25,092 / 3,155,379 bytes. These peaks are retained, not excluded. Serialization cost grows with retained history; the native persistence/high-history gates must still pass. No comparable native before/after measurement or gameplay overhead claim is available.

Continue the remaining [native qualification checks](M18_MANUAL_QUALIFICATION.md), including the corrected shortcut-isolation repeat, then complete recovery/degradation, all matched performance cells and the natural high-history soak. Native UI has the bounded screenshot and user-confirmation evidence above; complete UI/gameplay agreement remains partial, while measured native resources and performance remain **Not exercised**. The source `78265dc` access/storage and clean-shutdown checks passed as recorded below. The [release procedure](RELEASE_PROCESS.md), [prepared Workshop description](WORKSHOP_DESCRIPTION.md) and [local data guide](LOCAL_DATA.md) are ready. Workshop preview selection/subscription installation and merge/tag/release/upload authorization remain outstanding.

## Native qualification follow-up

Save 3 reached the base after the opening prison tutorial, and the user exported baseline revision 350 at `2026-09-08T10:29:26.1733502Z`. [Baseline evidence](M18_SLOT3_BASELINE.json) records the exact generation, export checksums and matching item/Cash totals. The baseline was exported on source `6125a2b`; subsequent replacement identities are recorded above. The native guide scene is a raid map, but the fresh native `RaidInfo` defaults to invalid and not ended; Duckov starts a new raid only after an ended prior record. Reaching base marks that record ended. This explains why the prior build recorded lifetime item use and Cash without a Run entry; the tutorial-only start correction below closes that coverage gap. The explanation is supported by native code/configuration and the reported sequence, not a captured tutorial `RaidInfo` trace. Regular expedition tracking still needs qualification. The prior no-active-run fallback labeled tutorial Cash `Base`. Future observed tutorial runs now provide Raid context; the historical baseline is retained as recorded. The user deferred empty-state text rendering polish until the checks are complete. The user subsequently confirmed Working Economy/Menu health labels and restored controls after closing Statistics, then reported that clicking the pause-menu entry did not display the panel. The callback activation alone did not establish visible opening.

On save 3, startup Diagnostics marked Menu access Limited solely because the base pause-menu entry was `NotObserved`. The presentation now reserves Limited for an observed unavailable entry. Unobserved and attached-but-unexercised rows retain their precise neutral status; the group remains Working, and the banner does not claim every entry was verified. The correction passed 50 focused Release Diagnostics tests, changed-file formatting/analyzers and a zero-warning installed-native Release build. The first prepared menu correction and its evidence are preserved in [the menu correction record](M18_MENU_HEALTH_CORRECTION.json); it was superseded before deployment by the combined Diagnostics correction below. Current correction delivery is tracked in [PR #19](https://github.com/bamboechop/ultimate-duckov-statistics/pull/19).

The same save's three Experimental Economy capabilities match the supported native contract: exact Money changes have only partial semantic source/context attribution, and proven external Cash acquisition does not cover corpse/container transfers or ambiguous player-originated split identities. Following the user's startup feedback, Diagnostics now treats this expected partial coverage as Working operational health. Capability state/provenance, unknown attribution, Economy data-coverage notices and exports are unchanged. Actual disabled/missing/conflicting capabilities still report Error, and unexpected Experimental capabilities remain Limited. This prevents documented measurement boundaries from becoming a permanent startup warning without hiding a loss of supported tracking. The combined replacement is prepared from source `6125a2b95592151765c3f85b8ca3288e8bd37308`. [Diagnostics correction evidence](M18_DIAGNOSTICS_HEALTH_CORRECTION.json) records 117 focused passes, 1,860 main plus 8 shell passes in each Debug/Release (two game-closed deployment tests excluded locally), a zero-warning native Release build, independent correction review, and matching ordinary-checkout/two-root artifacts. The exact five-file package and ZIP are under `artifacts/candidates/6125a2b95592151765c3f85b8ca3288e8bd37308`, with its frozen campaign under the matching `artifacts/qualification` directory. It supersedes the menu-only replacement. This combined replacement was deployed after user shutdown. All five destination hashes match, all nine v1 data files are unchanged, the prior candidate is retained for rollback and no transaction residue remains. Both previously excluded deployment tests subsequently passed in each configuration. The user subsequently confirmed Working Economy/Menu health labels and close/input restoration on this build. The separate pause-menu failure is addressed below.

The pause-menu failure was reproduced through the actual native UI integration: after closing, global canvas fallback selected the lower dialogue canvas instead of the live parent PauseMenu canvas. Source `1876468` resolves the owning ancestor for every base-menu access and marks the entry Available only when opening succeeds. Routine deferred world-time changes now show neutral Pending with Working storage after a matching-generation successful write; missing receipts and actual failures retain Limited/Error. Independent review identified a second write failure hidden by throttled logging; source `ef10fd0` exposes retained writer failure directly and refreshes open Diagnostics when it changes. The reviewed replacement was deployed with all 51 data files unchanged. The user later confirmed repeated pause-menu opening, restored controls and Working storage with neutral Pending writes on source `78265dc`. Full visual/resource qualification remains open.

The user's normal base-to-main-menu-to-quit sequence then exposed a false Economy pending warning. Cash observation correctly remains suspended in Main Menu, but the boundary result treated suspension itself as queued work. Source `78265dc` accepts suspension only after both actual queues drain. Three regressions failed before correction and pass afterward, including gate/publisher rejection, stable retry identities, no teardown scans and baseline-only hydration. Independent immutable correction review passed. The current replacement above was deployed while Duckov was closed, with the prior package preserved and all 51 data files unchanged. The user confirmed the three access/storage checks after restart. The subsequent normal shutdown at `2026-09-08T11:23:08.0510743Z` closed the generation cleanly without the Economy pending warning.

Source `9f503b7` starts the first tutorial run only in the verified `Level_Guide_Main` main scene while native `RaidInfo` is invalid, unended and alive and the existing raid/control/loading/pause/health guards pass. It uses the existing UDS GUID and leaves NativeRaidId absent. A consumed main-scene host prevents duplicate runs across the tutorial's two evacuation notifications. Native death, extraction, subscene routing, metrics and persistence are reused; no schema or Duckov save change is needed. Twelve regressions cover native composition, six fail against the prior implementation, and independent correction review passed. Current deployment is recorded above. The user subsequently completed a fresh tutorial on save 3 and supplied a screenshot and export. [Native qualification](M18_TUTORIAL_NATIVE_QUALIFICATION.json) verifies exactly one start and extracted completion, the guide-1/guide-2 route, complete run equality between saved profile and JSON, and displayed totals and targeted CSV agreement. The four kills partition into three ranged kills and one effect kill. Both segments share the native localized name Gefängnis but retain distinct stable map IDs. The earlier 55-Cash/zero-run export remains unchanged. Cold reopen of this run passed: one unchanged completed run apart from intentional removal of obsolete Economy replay cursors. Native tutorial death/retry remains open. The same restart exposed a separate lifetime Cash shutdown defect below.

The user confirmed no Cash action between tutorial export and direct quit/restart. Lifetime Cash nevertheless gained 58 Base `UnknownAdjustment` outflow while live holdings remained 58; the completed run remained +58. [Reopen qualification](M18_TUTORIAL_REOPEN_QUALIFICATION.json) preserves both session logs, the new export and closed profile. Direct Quit bypasses SceneLoader, and native inventory destruction can precede the final UDS cleanup scan. Source `f0187b2` subscribes to the committed application-quit event, flushes live changes and suspends further Cash scans while retaining pending Money/Cash events for retry. Both quit-notification orders, repeated notifications, gate/publisher rejection, attribution and subscription disposal are covered. Independent immutable review passed. The replacement was deployed while Duckov was closed with all 129 UDS data files unchanged. [Native direct-quit requalification](M18_DIRECT_QUIT_NATIVE_QUALIFICATION.json) passed: full application shutdown at `2026-09-08T12:16:32.6951840Z`, new activation at `2026-09-08T12:16:46.3628409Z`, exact completed-run retention, holdings 58 and no additional Cash outflow. The profile closed cleanly without a pending Economy warning. A preceding base-to-main-menu/reload also preserved the run and Economy totals. The existing false outflow remains preserved and has not been automatically repaired.
