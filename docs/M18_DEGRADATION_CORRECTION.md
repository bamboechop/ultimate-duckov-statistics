# M18 missing-Harmony corrections

The [native missing-Harmony check](M18_MISSING_HARMONY_QUALIFICATION.json) retained the selected slot 3 generation and its one completed sleep/3,540 seconds. Diagnostics truthfully marked affected tracking unavailable, independently supported siblings remained available, and UI export completed. The check exposed two defects that require a corrected native repeat.

- Overview World time disabled wrapping in a fixed-height card. The longer `capture incomplete` suffix extended beyond its right edge. This correction enabled wrapping and native preferred-height card growth, but later native activation overwrote the wrapping flag. The [follow-up initialization correction](M18_WORLD_TIME_INITIALIZATION.md) reapplies wrapping after activation and before measurement. Font size, full text, metric values and evidence qualifiers are preserved.
- Healing and combat retried a missing Harmony dependency once per second and logged an unchanged failure on every retry. Combat additionally republished unchanged capabilities, causing a complete profile write and, in an active run, a requested combat checkpoint. Each adapter now reports a repeated initialization failure only when its detail changes, clearing that state when patcher creation succeeds. Combat publishes failed-initialization capabilities only on first initialization or changed hook support. Existing retry timing, late activation, independently supported callbacks and changed failure/patch-drift reporting remain intact.

The original log preserves 233 exact repeated missing-Harmony messages over the native session from 18:03:27 to 18:05:24 UTC on 2026-09-08. The export, closed profile and both screenshots remain unchanged in the recorded evidence directory. Historical incomplete evidence is not repaired by this correction.

The focused regressions execute production Overview construction/layout with a retained 59-minute incomplete sleep at 2560x1440 and 720x480 plus resize, and the production healing adapter's failed patcher initialization/retry/recovery path. The test Harmony assembly is permanently loaded, so that retry regression uses registered cleanup failure to reach the same failure branch. Full combat failed-initialization publication is covered by source/native composition review and the requested native repeat; the test suite does not emulate the complete combat adapter or actual Harmony absence. Native glyph rendering remains a user check.

Complete Debug/Release main and shell suites, native builds and installed-contract probing passed before source freeze. [Delivery evidence](M18_DEGRADATION_CORRECTION.json) records source `adb5e706be1cfff93e0b97ece07399df8b9072b8`, 1,909 main and 30 shell passes in each configuration, independent complete nine-file review with no actionable P1/P2, reproducible DLL/PDB/ZIP bytes, and game-closed five-file deployment preserving all 2,241 UDS data files. The [subsequent native repeat](M18_MISSING_HARMONY_REPEAT_QUALIFICATION.json) passes retained-data/export and quiet-retry checks. The later explicit Overview screenshot overturns the earlier broad World time fit acceptance. [Harmony restoration](M18_HARMONY_RESTORATION_QUALIFICATION.json) passes; the [corrected World time repeat](M18_WORLD_TIME_NATIVE_QUALIFICATION.json) passes by user confirmation; other native gates remain open.

The native repeat preserved one sleep/59 minutes; its broad visual report did not establish World time fit, as the later screenshot demonstrates. Only startup dependency warnings remain; the finite revision progression supports removal of the repeated combat-publication flood, without tracing every physical write or qualifying active-raid checkpoint frequency. The [separate status-width correction](M18_DIAGNOSTICS_WIDTH_CORRECTION.md) addresses the new screenshot finding. The user has restored HarmonyLib and fully restarted; native capabilities and retained statistics pass. The subsequent [status-row fit check](M18_DIAGNOSTICS_WIDTH_NATIVE_QUALIFICATION.json) passes by user confirmation, as does World time on the corrected build with Harmony enabled. Historical capture gaps remain separate from current operational health.
