# M18 actionable Harmony diagnostics

When the current process has no loaded Harmony assembly, Diagnostics names that dependency for reported failures of Harmony-dependent statistics and shortcut isolation. The banner explains how to install or enable HarmonyLib and fully restart Duckov with Harmony loading before UDS. Affected system and metric rows retain their Error/Limited severity alongside `Harmony not loaded`, and affected Recent issues include the recovery instructions.

The observation uses the same assembly lookup as the native patcher, only when constructing an already-needed Diagnostics snapshot. It adds no per-frame assembly scan or persisted profile field. The exact capability catalog marks dependencies verified against the supported native adapters. Public item-use, firing, melee-swing, player-death, clock and other independent callbacks retain their actual status. Storage failures and unrelated failed metrics keep their own diagnosis. Missing/conflicting records do not establish a missing-dependency cause. Historical log messages never determine the current Harmony observation; native contract details remain intact for additional failures.

Presentation regressions cover the production combat policy with public callbacks and unavailable Harmony hooks, clock/sleep separation, unknown or restored runtime evidence, stale logs, missing/conflicting records, mixed failures, and unavailable shortcut isolation with working menu entries. The retained-panel foreign-patch regression checks the actual assembly observation and verifies that a loaded Harmony conflict is not labeled missing. The test Harmony assembly cannot be unloaded: these tests validate presentation decisions and production wiring, while actual missing-Harmony rendering remains a native qualification step.

The [earlier wrapping and retry corrections](M18_DEGRADATION_CORRECTION.md) remain in this candidate. Combine their outstanding repeat with this presentation check: keep HarmonyLib disabled, launch slot 3, confirm that Diagnostics names Harmony and shows the restart instructions, inspect affected and independently working rows, and check Overview at 720x480 and after resizing. Leave Diagnostics open at least 15 seconds, export and close Duckov. Preserve that degraded result before enabling HarmonyLib and fully restarting for the restoration check. Existing incomplete historical evidence must remain truthful after restoration.

M18 remains open until the native repeat, restoration, performance captures and other acceptance gates are qualified. Delivery identities and completed automated results are recorded separately from native acceptance.
