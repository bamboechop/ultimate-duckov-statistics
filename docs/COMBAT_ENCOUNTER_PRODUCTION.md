# Encounter history production integration

The ordinary mod now captures encounter history automatically and exposes **Details | Map & Kills** in Runs. No encounter build flag or recording hotkey is needed. This is unreleased source work; the published GitHub 1.0.0 archive is unchanged. The user's accepted design includes overlapping markers when encounters occur at the same position. Do not spread or cluster them artificially.

## Runtime and diagnostic separation

`Encounters/EncounterCaptureHost.cs` owns native combat, corpse/loot and route observers, the bounded detached observation pipeline, incremental publication and run/profile barriers. Native callbacks and their trusted Harmony patch sets are identical between normal and diagnostic builds. Existing fail-closed coverage, publication retries and independent aggregate tracking remain in place. Shared health/corpse callbacks keep their existing owners; encounter-only observers use dedicated encounter owners.

The retained Runs view, map geometry/artwork and presentation code are ordinary code under `Encounters/` and `UI/`. Map artwork remains in the existing `UltimateDuckovStatistics/encounter-prototype/maps/maps-cache` directory; the historical directory name is retained deliberately so previously recorded maps remain available after restart. There is no data migration, profile reset or map-cache copy in this integration.

Raw JSONL recording, its F6 control, the F5 saved-capture viewer, replay-file reader and diagnostic overlay are under `Encounters/Diagnostics/` and compile only with `UDSEncounterDiagnostics=true`. The normal build does not reserve F5/F6. Performance diagnostics remain independently controlled by `UDSPerformanceDiagnostics`. The ordinary package audit rejects both the obsolete prototype namespace and the optional encounter diagnostics namespace.

Use `scripts/build.ps1` and `scripts/package.ps1` for ordinary builds/packages. `scripts/build-encounter-diagnostics.ps1` creates a separate, clearly labeled diagnostic package with both diagnostic flags enabled; it must not be published as a release. All packages retain the existing 13-file inventory and do not redistribute game assemblies.

## Validation boundary

Source-linked tests exercise the real capture reducer, storage/export/restore, map/feed projection, coverage handling, native combat/loot observers and aggregate adapter callback composition. Native objects, callback invocation and host sink routing remain simulated boundaries; shell tests also stub the Runs child view. Neither suite simulates Unity rendering or independently proves native actor/loot discovery. The native build, installed contract probe and exact package audit complement those tests.

The focused ordinary-build test requested automatic capture without F6, existing history/maps after restart, linked map/feed selection, retained loot tooltips, English/German switching, JSON export and perceived frame pacing with the panel closed/open. The user reported this gameplay test passed on September 16, 2026, after the review corrections. This is user acceptance of that workload, not an independently instrumented long-run benchmark. Failure injection and incomplete-recording states retain their automated evidence; no failure was deliberately induced in the user's profile. See the [reviewed-build acceptance](#september-16-2026-reviewed-build-acceptance) below and [PR #25](https://github.com/bamboechop/ultimate-duckov-statistics/pull/25) for authoritative merge and CI status.

## September 15, 2026 integration checks

The final main suites passed 2,390 tests each in Debug and Release. Ordinary shell suites passed 98 each, including assigning F5/F6 as normal panel shortcuts; the combined diagnostic shell passed 100. One initial Debug allocation assertion failed, then passed in isolation and in the complete final Debug run without a production or test-policy change. The installed Duckov contract probe passed. Ordinary Debug/Release, encounter-only diagnostic and combined diagnostic native builds passed with zero warnings/errors. The ordinary package audit verified capture startup/update and Runs rendering call sites, required encounter types, the absence of diagnostic types/calls, and all 13 package files. A real encounter-only diagnostic assembly was correctly rejected by the ordinary audit.

The ordinary package is `artifacts/encounter-history/production/20260915T2141082095881Z-0b898cafa37f459ab4eb49cd2b9b543e/`. It was deployed with Duckov closed, a verified prior-mod backup retained, and all 13 installed hashes read back. Native DLL SHA-256: `a693b16aa1446cc3ab3d5b5a95347e7efa9d61bbbfd6492720e2c7d1d7b00d14`. Source/package inventory, deployment and validation receipts accompany it. The package came from then-uncommitted encounter branch work and was not a published release. The user subsequently reported that the ordinary build looked good. Independent review and final feature acceptance were still pending at that checkpoint; the September 16 corrections and acceptance are recorded below.

## September 16, 2026 review corrections

The independent review identified three reachable defects. The corrections are:

- Effect observations now carry the shared combat adapter's resolved ownership into encounter capture. Mixed contributors and unavailable ownership cannot promote a retained native buff actor/weapon into player damage or kill credit. Incoming damage remains recorded with unknown attribution. Unresolved source positions are not presented as proven attacker locations.
- Pause/loading, temporarily missing characters and character replacement retain the current map visit and resume through a route gap. A new visit requires a changed visit identity. A combat observation arriving before the map observer's first update also shares that visit rather than creating a duplicate selector entry.
- Focused encounter maps project player/enemy endpoints independently. A known endpoint remains visible and receives automatic focus when its counterpart is absent or on another map. The connector and distance require both endpoints; closing selection restores the overview.

Regression coverage exercises the real shared effect callback with same/mixed actors, the encounter reducer's damage/fatal credit, native map-observer Tick through pause/loading/character boundaries and the real reducer, and the focused-map projection used by the retained view. Native actor discovery, map-art rendering and Unity pointer/display behavior remain separate from the stubbed native boundaries used in tests. Existing recorded runs are not rewritten to reconstruct evidence that was not captured.

After these corrections, all 2,412 main tests and 98 ordinary shell tests passed in both Debug and Release, including 22 added regression cases. The installed native contract probe, ordinary Release assembly/package audits and 13-file package verification passed. Ordinary Debug/Release and combined encounter/performance diagnostic Debug builds completed with zero warnings/errors. The build log is retained locally at `artifacts/encounter-review-fixes-build.log`; the source inventory is under `artifacts/encounter-review-fixes/`. These checks do not establish in-game acceptance of the corrected build or an independent re-review.

### Follow-up review: destroyed shooters and death-marker positions

A subsequent review identified two additional defects. Actor lookup now checks its weakly held identity cache using managed-reference semantics before requiring a live Unity object. A projectile can therefore retain its previously observed shooter after native destruction, without inventing a current shooter position or identifying an actor that was never observed. The actor-lookup implementation is source-linked into the tests, including simulated Unity destroyed-object equality and reduction of a projectile hit/player death after the shooter's own death.

Overview markers now use the fatal victim's endpoint: the player for player deaths, the enemy for enemy deaths. A missing or unmappable victim position does not borrow the other endpoint as a death location; list selection and independently available focused pins still work. Regression cases cover all three outcome types, distinct endpoint locations and missing victim positions.

Eight additional regression cases passed with the full 2,420 main tests and 98 ordinary shell tests in both Debug and Release. Ordinary Debug/Release and combined diagnostic Debug builds passed with zero warnings/errors, as did the native contract probe, ordinary assembly/package audits, 13-file package verification and `git diff --check`. The build log is `artifacts/encounter-review-followup-build.log`; source/validation receipts are under `artifacts/encounter-review-followup/`. This follow-up was packaged without deployment; native gameplay/rendering acceptance and an independent review of the corrected source remain separate.

### Follow-up review: shared combat-hook trust

Encounter capture now consumes the shared combat owner's hook trust and resolved projectile/melee scopes. A rejected scope shadows any enclosing encounter attack instead of borrowing its attribution, and the finalizer restores the enclosing scope. Projectile lifecycle degradation clears encounter launch correlations alongside the shared owner's invalidation. Hits lacking a trusted scope retain independently observed health-callback evidence without claiming projectile ammunition or a zero headshot count. Release-hook loss still permits fresh, trusted Init/Update evidence; unrelated effect equipment-association loss does not disable encounter projectile or melee evidence.

Shared health, projectile, melee, effect and buff-ownership degradation emits persistent incomplete-combat coverage for each affected run, including a run started after the hook became unavailable. Capture of independently reliable evidence continues. Existing runs are not rewritten.

The combat observer itself is now source-linked into the native-boundary test assembly. Twelve new regression cases exercise real shared callbacks and observer code, foreign-patch detection, old/new projectile correlations, nested scope restoration, independent combat families, repeated-run coverage and record-codec/presentation readback. Native objects, Harmony invocation and host sink routing remain test boundaries; these checks do not simulate game physics or establish in-game acceptance.

All 2,432 main tests and 98 ordinary shell tests passed in both Debug and Release. The existing allocation-free ownership-policy test failed in the first full Debug run, then passed in isolation and in the full rerun without changing code or its assertion. Ordinary Debug/Release and combined diagnostic Debug builds completed with zero warnings/errors. The installed native contract probe, ordinary assembly/package audits, 13-file package verification and whitespace check passed. Logs are `artifacts/encounter-review-hook-trust-build.log` and `artifacts/encounter-review-hook-trust-build-initial.log`; source/validation receipts are under `artifacts/encounter-review-hook-trust/`. This correction was packaged without deployment or commit. Native acceptance and independent re-review remain separate.

### Follow-up review: shared corpse-hook trust

Encounter loot capture now checks whether the shared container owner can still provide trusted corpse provenance. Startup rejection, runtime patch drift and owner detachment block new death-to-corpse associations and report persistent incomplete-loot coverage in each affected run. Pending death associations are discarded when trust is lost. Previously verified corpse identities and the encounter-owned inspection/transfer hooks remain usable; a shared provenance failure does not erase their observations or stop independently reliable looting evidence.

The native container adapter, shared callbacks and complete loot observer are source-linked into the tests. Eight regression cases cover pre-existing conflicts on either shared hook, runtime drift on either hook, loss during an open death scope, owner detachment, continued inventory/transfer evidence for an already verified corpse, normal healthy capture and coverage readback through the record codec. Native objects, callback invocation and host sink routing are simulated; these tests do not claim native UI or gameplay acceptance.

All 2,440 main tests and 98 ordinary shell tests passed in both Debug and Release. Ordinary Debug/Release and combined diagnostic Debug builds passed with zero warnings/errors, as did the installed native contract probe, ordinary assembly/package audits, 13-file package verification and whitespace check. The build log is `artifacts/encounter-review-loot-trust-build.log`; source/validation receipts are under `artifacts/encounter-review-loot-trust/`. The corrected package was not deployed or committed. Independent re-review and native acceptance remain separate.

### Follow-up review: simultaneous fatal-event ordering

Encounter outcomes now retain the native observer's fatal sequence. The whole-run feed and each visit's map events use it to break equal-time ties before the deterministic record-ID fallback. Actor discovery order and nested callback completion order therefore no longer decide the numbering of deaths sharing the tracker's last-advanced elapsed time. The sequence survives record encoding, SQLite, export and restore, and becomes immutable with the fatal outcome. Records without this evidence keep it absent; existing history is not rewritten to invent an order.

Ten regression cases cover source-linked shared health callbacks with synchronous explosion victims, opposite actor discovery order, nested death completion, numeric tie-breaking, timestamp precedence, both record codecs, repository reopening, ZIP restore and invalid/mutated order evidence. The native callback tests simulate native health assignments and callback invocation; they do not constitute in-game acceptance.

All 2,450 main tests and 98 ordinary shell tests passed in both Debug and Release. Ordinary Debug/Release and combined diagnostic Debug builds passed with zero warnings/errors. The installed native contract probe, ordinary assembly/package audits, 13-file package verification and whitespace check passed. The build log is `artifacts/encounter-review-fatal-order-build.log`; source/validation receipts are under `artifacts/encounter-review-fatal-order/`. This correction was packaged without deployment or commit. Independent re-review and native acceptance remain separate.

### Follow-up review: unscoped ownership after effect-hook loss

When the shared `Effect.Trigger` hook loses trust, an unscoped health callback no longer treats its native actor and weapon fields as proven ownership. Some native effect actions, including `ExplosionAction`, copy a retained buff actor without setting `isFromBuffOrEffect`. Encounter capture now follows the aggregate combat owner's conservative fallback: health evidence remains observable, but uncertain damage/kill credit, weapon identity and attacker positions remain unknown. Independently trusted projectile and melee scopes still provide their normal attribution and positions. Explicit unscoped effect damage also remains unresolved.

Eight source-linked regression cases cover incoming/outgoing damage, native effect flags present/absent, hook loss before observer creation and during observation, healthy unscoped explosions, independent projectile/melee attribution, and record-codec/presentation readback. Before the correction, four cases reproduced false attacker positions; the corrected assertions also check unknown final credit/weapon, no false outgoing player damage, retained incoming health loss and persistent incomplete-combat coverage. The before-fix log is `artifacts/encounter-review-fallback-before.log`. Native health transitions and callback invocation are simulated; these checks do not establish native gameplay acceptance.

All 2,458 main tests and 98 ordinary shell tests passed in both Debug and Release. Ordinary Debug/Release and combined diagnostic Debug builds passed with zero warnings/errors. The installed native contract probe, ordinary assembly/package audits, 13-file package verification and whitespace check passed. The build log is `artifacts/encounter-review-fallback-ownership-build.log`; source/validation receipts are under `artifacts/encounter-review-fallback-ownership/`. The corrected package was not deployed or committed. Independent re-review and native acceptance remain separate.

### Follow-up review: grenade-zone ownership

The installed `ZoneDamage.Damage` clears `DamageInfo.fromCharacter` before invoking `Health.Hurt`. Encounter capture now receives the shared environmental callback's resolved grenade thrower and item, just as it receives shared effect ownership. This preserves damage and fatal credit even for an enemy that was not previously related to the player. Unknown or rejected zones shadow enclosing attacks and restore them on exit; they cannot borrow projectile attribution. Loss of grenade-origin or environmental-hook trust reports persistent incomplete combat coverage while independent attack families remain available.

Nine regression cases reproduced the defect before correction and passed afterward. All 57 source-linked native combat degradation tests passed in Release, including codec/history readback, incoming/outgoing damage, previously related enemies, four hook-loss paths and nested scope restoration. The ordinary native Release build completed with zero warnings/errors. Logs are `artifacts/encounter-review-environment-before.log` and `artifacts/encounter-review-environment-focused.log`. These checks simulate native callbacks and health transitions; native gameplay acceptance remains separate.

### Follow-up review: missing route context

When the lifecycle tracker's 64-segment bound is exhausted, the run remains active without a current segment. Encounter reduction now records a run-owned `ContextUnavailable` coverage notice when observations cannot be attached to a known map/segment. It seals the previous visit, retains earlier evidence and does not invent a replacement segment. Repeated discarded observations do not repeat the notice; valid context can resume capture in a new visit. Loading/discontinuity notifications without segment context still break the path within the prior visit and do not by themselves claim lost gameplay evidence.

Seven additional regression cases cover the real tracker reaching its segment limit, rejected combat/loot/path observations, missing map or segment context, deduplication across worker batches, later-run ownership, resumed capture and loading gaps. The durable-notice test also covers storage retry, SQLite reopening, ZIP export and restore. Six cases failed before correction; all 236 focused encounter tests passed afterward. Logs are `artifacts/encounter-review-context-before.log` and `artifacts/encounter-review-context-focused.log`.

After both the grenade-zone and route-context corrections, all 2,474 main tests and 98 ordinary shell tests passed in both Debug and Release. Ordinary Debug/Release and combined diagnostic Debug builds completed with zero warnings/errors. The installed native contract probe, ordinary assembly/package audits and 13-file package verification passed. The complete build log is `artifacts/encounter-review-zone-context-build.log`; source and validation receipts are under `artifacts/encounter-review-zone-context/`. These automated checks do not establish native gameplay/rendering acceptance; no deployment was performed during this correction.

## September 16, 2026 reviewed-build acceptance

After the user ended the review phase, the ordinary package from committed source `44a4446d06fe6046fb876e0a50e3113e936f3fc4` was deployed at `2026-09-16T14:27:20.9454502Z` with Duckov closed. All 13 installed file hashes matched the package, and all 30 existing UDS data files were unchanged by deployment. A verified copy of the previous mod was retained. The source/package and deployment receipts are under `artifacts/encounter-history/production/20260916T1427198719483Z-44a4446/`; native DLL SHA-256 is `48babea14625ced5870afd279ae8523d2035a0ed10420067ba1b1f72e25f9dcd`.

The user subsequently reported the focused gameplay test was green. The new `statistics.zip` export at `20260916T1448054360820Z-a689e706105649ec996a6007ffb457e6` contains run `c9445bb675e642a0b623996a9daacee1`. Its Man of Light encounter retains projectile and effect damage separately, including 8 effect damage attributed to the M700 and 10 attributed to the incendiary item. The exact effect name is not recorded, so the M700 row does not by itself prove bleeding.

This closes the feature's focused native acceptance following the automated validation above. It does not claim exhaustive enemy/loot coverage, live fault-injection acceptance or a new controlled long-run frame-time benchmark. The acceptance documentation changes no runtime code and requires no redeployment. Merge status belongs to [PR #25](https://github.com/bamboechop/ultimate-duckov-statistics/pull/25); exact release packaging and publication follow the [release process](RELEASE_PROCESS.md). The existing GitHub 1.0.0 archive was not replaced by this acceptance work, and no Workshop publication was performed.
