# M18 manual qualification

Use the candidate source and file identities in [M18 acceptance](M18_ACCEPTANCE.md). Automated isolated recovery/reset tests are complete only when that record says Pass. They do not prove native gameplay, rendering or frame-time acceptance.

## First native check

1. Launch Duckov yourself. Confirm HarmonyLib and exactly one UDS instance are enabled before selecting your chosen save. The new `v1` directory should begin with a fresh UDS generation; old development profiles remain untouched.
2. Outside a raid, open Statistics from the main menu, base pause menu and F8. Visit all nine tabs, change language, resize/change resolution, scroll wide tabs and long content, and verify Back/Escape, keyboard focus and the native pause menu after closing. Repeat open/close and a menu round-trip. In a raid, the unavailable-access message is expected.
3. On the selected save, perform a short ordinary sequence: one successful consumable use and healing where available, firing and an observable enemy outcome, container access, equip/unequip, a paid craft and economy change, and a completed sleep. Keep exact action notes. Do not add unsupported actions just to fill a checklist.
4. Complete an expedition with at least three map visits, including a repeated map if naturally available. For delayed effects, record the source, any loadout swap and the later outcome when such an item is actually available. Extract, inspect Runs and the relevant tabs, then export through Diagnostics.
5. Close the game normally yourself. Codex can read the local logs, generation, checkpoints, profile/backup and exports, compare JSON/CSV/UI against the action notes, and verify clean shutdown and exact deployed hashes.

For tutorial coverage, use a user-selected fresh save. After gaining control, record a few exact actions and finish the prison exit countdown. Verify one completed run for the observed tutorial map, matching actions and Cash, then compare an export and cold reopen. The first attempt has a UDS run ID and no native raid ID. If death occurs, that attempt should end as Died and the retry should become a separate run with Duckov's newly created raid ID. The original save-3 generation remains the zero-run baseline; a subsequent fresh generation has a verified two-segment tutorial extraction and completed-run cold reopen. No historical duration or movement is reconstructed.

## Direct-quit correction check

Passed on save 3 at `2026-09-08T12:16:32.6951840Z` with cold-restart export afterward; [qualification evidence](M18_DIRECT_QUIT_NATIVE_QUALIFICATION.json) records unchanged run/Cash totals and clean closure. The procedure below remains available for future regression checks.

On the deployed correction, launch save 3 and export without gameplay. Quit directly from the base pause menu, relaunch the same save and export again. The generation, completed run, Cash holdings and lifetime Cash flow totals must be unchanged. The existing erroneous 58 Cash outflow remains in the preserved baseline; the correction must not add another outflow. Compare the two exports and shutdown log against [the correction evidence](M18_DIRECT_QUIT_CASH_CORRECTION.json).

## Current-language names repeat

The reported 720x480 layout passed; English exposed saved German entity labels. The [name correction](M18_NATIVE_NAME_CORRECTION.json) is deployed and the user confirms language switching works. [Native repeat evidence](M18_NATIVE_NAME_QUALIFICATION.json) also verifies cold-reopen/profile/export retention. The following procedure is retained for future regressions. On slot 1, open Equipment and Combat in English, then inspect a recorded run's map/route names. Switch back to German and reopen the same views. Names should follow Duckov's current language while counts and durations stay unchanged. Missing native metadata retains its recorded label; UDS interface text remains English. Export once after the check so startup and profile retention can be compared with the preserved pre-deployment snapshot.

## Native shortcut isolation repeat

F8 opening/reopening, Ctrl+Tab cycling, hotkey-capture cancellation and restored movement passed. The user found that Tab still opened Duckov inventory above UDS. The [scoped correction](M18_SHORTCUT_CORRECTION.md) guards inventory, map, quest and reload callbacks while the panel owns input; the user confirms the requested repeat works. [Native qualification evidence](M18_SHORTCUT_QUALIFICATION.json) records the bounded input report, cold reopen and profile/export retention. The following procedure remains for regression checks. At base, use F8, test plain Tab and both Ctrl+Tab directions, then the configured map/quest/reload shortcuts. The native actions should stay inactive. Escape should cancel hotkey capture without closing UDS; a second Escape should close UDS. Verify the ordinary native shortcuts and movement after closing. Export after the repeat. Broader deferred input coverage and resource/performance checks remain separate.

## Persistence, degradation and ownership

The user selected slot 3 for native reset/recovery checks. [Reset evidence](M18_SLOT3_RESET_QUALIFICATION.json) verifies one new generation, an archive preserving both prior runs, zero recorded actions in the fresh profile, retained current holdings and unchanged slot 1 statistics/settings. The user confirms Cancel retained both runs and confirming reset showed an empty Runs page. The reset procedure remains available for regression checks: first cancel and verify both runs remain, then confirm once and export. Slot 1 remains intact for later history/performance qualification. The subsequent 59-minute sleep is verified in the export and byte-identical closed primary/backup. [Backup-recovery evidence](M18_SLOT3_BACKUP_RECOVERY_QUALIFICATION.json) records the copied-backup production rehearsal, reversible native primary holdout and successful native repeat. Startup recovered the same generation without an interruption and repaired its primary; the user confirms recovery/sleep UI, and saved-profile/JSON/all 36 CSV comparisons retain one sleep/59 minutes. Both archived runs, all nine slot 1 runs and settings remain intact. The original primary remains preserved as evidence; do not restore it over the newer recovered profile. This passes missing-primary backup recovery and reset-generation cold reopen; other native recovery scenarios remain open.

- Cold reopen the same current-format profile and verify exact totals/generation. Reinstalling the same package while closed must preserve those totals. A second user-selected save gets its own generation and cannot borrow data.
- Exercise reset only in an explicitly chosen disposable UDS test generation. Confirmation defaults to Cancel; successful reset archives only UDS data, and export/reset cannot run twice concurrently. Real save deletion/reuse, a forced process termination or real-profile reset requires a separate deliberate user choice. Isolated fixtures cover primary/backup/temporary corruption, interrupted writes, generation handoffs, blocked checkpoint/profile writes and rollback without touching real saves.
- The [disabled-Harmony repeat](M18_MISSING_HARMONY_REPEAT_QUALIFICATION.json) passes for the actionable explanation, retained one sleep/59 minutes, supported siblings and usable export, with startup-only dependency warnings. [Harmony restoration](M18_HARMONY_RESTORATION_QUALIFICATION.json) passes after a full restart. The explicit World time overflow failure is preserved; the [corrected viewport/resize repeat](M18_WORLD_TIME_NATIVE_QUALIFICATION.json) subsequently passes by direct user confirmation, export agreement and clean shutdown. The [status-width correction](M18_DIAGNOSTICS_WIDTH_CORRECTION.md) passes its [native visual check](M18_DIAGNOSTICS_WIDTH_NATIVE_QUALIFICATION.json): the user confirms `Error · Harmony not loaded` fits on one line at the requested 2560x1440 size. No new gameplay or export was requested for that label check. Harmony must be enabled before performance qualification. Native active-raid checkpoint rates and foreign-conflict behavior remain separate; do not introduce an arbitrary conflicting mod into controlled performance captures.
- Check repeated native setup/deactivation and scene/profile changes for duplicate subscriptions, UI roots, input blockers and repeated errors. Managed shell tests cover ownership at isolated boundaries; native object/resource behavior remains a separate observation.

## Performance session

The two reported layout corrections and dependency restoration check are passed. The first six performance samples were captured at the same user-confirmed Nullpunkt location; the initial base label was corrected with all originals preserved. [Raid-idle qualification](M18_RAID_IDLE_QUALIFICATION.json) misses the engineering target at 6.36750% median / 19.99693% p99 overhead. The [equipment-duration correction](M18_IDLE_PERFORMANCE_CORRECTION.json) has a reproducible replacement package and a new frozen campaign. Both game-closed deployment checks passed in each configuration, and the replacement is deployed with exact readback and all 2,356 UDS files unchanged. The [restart/export check](M18_IDLE_CORRECTION_REOPEN.json) passes. The [fresh six-sample repeat](M18_IDLE_REPEAT_QUALIFICATION.json) passes the 10% p99 target at +6.48665% but misses the 5% median target at +5.96431%. Both valid isolated hitches remain included; the initial paused baseline attempt is separately preserved as invalid. The [completed diagnostic](M18_NATIVE_IDLE_TIMING_RESULT.json) identifies RunLifecycle as the dominant measured Update component, with no instrumented effect callbacks in the interval. The [reviewed duration-bound correction](M18_DURATION_BOUND_CORRECTION.json) is packaged with complete automated checks and an unchanged frozen capture matrix. The ordinary package is now deployed after explicit closure confirmation, with all installed files verified and all 2,397 UDS files unchanged. The [native reopen/export check](M18_DURATION_BOUND_REOPEN.json) passes with the same generation, both Nullpunkt extractions, unchanged economy totals and green Diagnostics. At the user's request, the [new comparison](M18_DURATION_BOUND_IDLE_COMPARISON.json) first reused the latest existing B runs 2/3/4 and collected three new ordinary D samples. Median +6.20728% remains above the 5% target; p99 +1.95090% is within 10%. All samples and both isolated hitches remain included. This does not establish a new matched pass or a median improvement. The [completed three-B reconciliation](M18_IDLE_CONTROL_RECONCILIATION.json) retains all current D samples and passes the bounded idle targets at +2.32632% median / +1.74807% p99. All 46,501 frames and isolated hitches remain included, with no clusters or increased D material-frame rate. Both old and new comparisons remain preserved. Continue to the declared firing cells with Harmony-only still enabled. The user reports SR-3M 30 rounds loaded / 320 spare; use one full 30-round burst into clear empty space, with no reload during timing. Do not start a capture from a setup-only Ready without the explicit timed procedure. Preserve every prior valid sample; managed timing does not qualify the replacement. Base-idle and the other declared cells remain unexercised. Earlier re-enabling UDS after unobserved native saving archived the nine-run generation under the documented save-continuity policy. The latest intervening UDS-disabled launch similarly archived the following two-run generation, preserving both complete runs unchanged. Current D uses a fresh generation with zero completed runs; progressed-history work remains separate. The user controls game launch, mod enablement, actions and captures.

The [completed SR-3M empty-space firing check](M18_HIGH_RATE_EMPTY_QUALIFICATION.json) retains three valid B and three valid D captures plus the separately preserved invalid first-beep attempt. It misses whole-capture median and firing-window median/p99 engineering targets, despite passing the numeric weaker ceiling and recording no action frame above 16.7 ms. The user subsequently paused direct-callback diagnostics and speculative optimization, and resumed bounded performance qualification using the existing 10% / 20% ceiling and hitch rules. Preserve the tighter-target misses and do not repeat a baseline solely to seek a favorable result.

The [completed SR-3M regular-duck check](M18_HIGH_RATE_SINGLE_QUALIFICATION.json) passes the bounded engineering/ceiling/spike gates across three B and three D captures. All 42,710 frames remain included, including a 511.189 ms D3 action frame with no repeatable cluster. The user confirms all six attempts, three ducks killed per configuration and no perceived hitch. The completed D run and profile/JSON/CSV agree on 90 shots, 90 completed projectiles and three SR-3M kills; 37 export files are preserved. No further capture is required for this short-history cell. The subsequent Med-Kit (S) workload is completed below. The original remaining cells are not silently removed or marked passed.

The [completed Med-Kit (S) check](M18_MEDKIT_QUALIFICATION.json) passes the bounded engineering/ceiling/spike gates across three B and three D captures. All 42,263 frames remain included; D contains no frame above 16.7 ms. The user reports no hitch, and profile/JSON/CSV agree on three uses and 36 HP restored (36.000000953674316 internally), with 75 durability consumed. Both configurations are unarmed and unequipped. The change to consecutive uncapped healing was recorded before B2; D started 6.1 HP lower, disclosed before D1. No repeat is required for this bounded cell. The subsequent base UI result is recorded below; UI/export on progressed history and the remaining original cells stay open.

The [base UI comparison](M18_BASE_UI_QUALIFICATION.json) **fails both engineering limits and the 10%/20% ceiling**: whole median/p99 +18.47977%/+23.10336%, action +21.59640%/+36.28332%. D1/D2/D3 and replacement B4/B5/B6 retain all 32,721 frames. Both configurations remain paused; D opens UDS via its injected pause-menu button, visits the nine tabs, scrolls Diagnostics and closes with F8. B takes no inputs. The initial B batch is excluded solely for the user-reported missing focus/abort, with every original file preserved. The user confirms focused, paused, no-input replacement controls and no perceived hitch. D1 has a disclosed late close; D2/D3 are fully cue-confirmed and each also exceeds both action ceilings against the B aggregate. All objective spikes remain, including the 766.205 ms D2 action frame. No further capture starts from this confirmation. The bounded source inspection identifies construction, change-triggered refresh, tab/layout work and cleanup as paths for targeted timing, without attributing the observed miss to a method. The [one completed UI timing diagnostic](M18_UI_TIMING_RESULT.json) records 29.984 seconds with one open, eight tab changes and one close; the user reported no perceived hitch. Synchronous Update averages 0.005571 ms closed / 0.062232 ms open, with no projection refresh while paused. Opening takes 29.4576 ms and closing 3.976 ms. The fresh zero-run profile differs from the original one-run acceptance, and deferred Unity work remains unisolated. These observations do not explain the earlier gap or justify a runtime correction. The investigation ended under the declared stopping rule. After fresh user-confirmed closure, [restoration of the frozen ordinary 88efddb package](M18_UI_TIMING_DIAGNOSTIC.json) passed both deployment checks in Debug/Release, exact five-file readback, unchanged hashes for all 2,528 UDS data files, and verified diagnostic rollback. No further diagnostic/baseline loop or acceptance exception is inferred. The original limits, failed result and remaining history/resource gates stay visible.

The current rules are in [PERFORMANCE.md](../PERFORMANCE.md) and [M18_CAPTURE_MATRIX_NO_FOCUS_DELAY.json](M18_CAPTURE_MATRIX_NO_FOCUS_DELAY.json). The [original matrix](M18_CAPTURE_MATRIX.json) and completed idle evidence remain unchanged. At the user's request, the separate follow-up campaign uses zero added focus delay. After explicit timed readiness and preflight, the capture starts immediately; its five-second pre-action measurement and action/end signals remain unchanged. The prepared `campaign.json` binds the matrix to the reproducible ordinary candidate's DLLs. Each cell needs three valid Harmony-only B captures and three candidate D captures. Confirm the exact retained weapons, equipment, save/location and graphical controls before the first capture; earlier M8.1 inventory notes are not current inventory evidence.

Copy [the controls template](M18_CONTROLS.template.json) to the local campaign directory and fill every material field. Use `capture-m18.ps1 -CampaignPath <campaign.json> -ControlsPath <controls.json> -Configuration B -Scenario idle -Run 1 -DuckovPath $env:DUCKOV_PATH -ValidateOnly` for preflight; omit `-ValidateOnly` only when the user is ready to capture. This does not launch or control Duckov. For non-weapon cells record the actual equipped weapon/ammunition and explicitly state zero expected shots.

After the matched matrix, preserve one untrimmed natural multi-map/high-history soak with UTC start/stop evidence and named early/middle/late windows. Record retained run count, segment/item/loadout cardinalities, profile size and memory/resource observations. Repeat the base UI/export cells on the progressed v1 history. No synthetic profile is injected into real gameplay. If sufficient history or a reproducible target is unavailable, keep that gate Not exercised and state the concrete limitation.

During every controlled capture the agent suspends builds, tests, indexing and other work. Resume analysis only after the user reports capture completion. Analyze approved valid raw inputs with the existing analyzer and `--campaign <campaign.json> --baseline B`; preserve all other attempts with objective invalidation reasons. A changed DLL requires a new frozen campaign and affected requalification.
