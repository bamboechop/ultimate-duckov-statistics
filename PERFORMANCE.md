# M8.1 performance protocol

This document freezes the M8.1 native benchmark method before runtime corrections are selected. Raw captures and their machine-specific sidecars live under ignored `artifacts/performance/captures/`; accepted results and hashes are summarized in `TESTING.md`.

## Capture method

- Collector: installed CapFrameX `1.8.6.2`, using its loopback capture API and bundled PresentMon. NVIDIA FrameView SDK `FvSvc`, OCAT, FrameView, and every other PresentMon-based collector must be stopped. CapFrameX sensor logging must be Off. Its overlay must be effectively unavailable or disabled: this machine has no RTSS installation or process, so the UI's configured `Overlay: On` state cannot render an overlay and is accepted only while that absence is reverified and recorded.
- Wrapper: `scripts/capture-frame-times.ps1`. It targets the one running `Duckov` process, verifies CapFrameX/API/process/settings state plus actual `Mod Loaded:` and UDS activation evidence from the current launch, refuses to overwrite evidence, and requests an explicit `JsonCsv` capture. Each accepted run preserves the raw CapFrameX CSV, raw CapFrameX JSON, and a `.capture.json` control sidecar.
- CapFrameX request: process `Duckov`, 10-second focus delay, 30-second timed capture, explicit `JsonCsv` output, an isolated staging directory, and a unique configuration/build/scenario/run comment. The wrapper validates the returned process, comment, one-run structure, and raw `MsBetweenPresents` rows before publishing the three artifacts.
- Analyzer: `tools/FrameTimeAnalyzer`. It accepts either the wrapper's raw PresentMon CSV or CapFrameX's native `.capframex.json` raw arrays. For CSV it requires a raw frame-time column, skips CapFrameX's leading `//Ignore=true` marker, excludes dropped rows, selects the dominant swap chain, and prefers CapFrameX's `CPUStartQPCTimeInMs` clock for correlation. Native JSON uses CapFrameX's already-selected run arrays (`MsBetweenPresents`, relative `TimeInSeconds`, `CpuActive`, `GpuActive`, and `Dropped`) and rejects missing, multiple-run, or length-mismatched evidence. Both paths report individual captures, isolated action windows, optional named time windows, and the same distributions and spike rules; controlled cells aggregate medians of per-run metrics rather than concatenating captures. Optional raw CPU/GPU-busy distributions are diagnostic evidence for CPU-versus-GPU isolation; release gates remain based on presented frame times.
- Quantiles: linear interpolation at `(n - 1) * p`. Average FPS is `1000 / mean frame time`; acceptance is based on frame times rather than the Steam overlay.

The schema-5 sidecar records the exact CapFrameX executable/version/hash, API request/response and relevant settings, both raw-artifact hashes, Duckov/game build, process start, repository commit, deployed package hashes, Harmony version/hash, active mods, save generation, route/association state, controlled background-application state, action kind, weapon/ammunition, complete equipment/totem state, action window, graphics preset, resolution, display mode, refresh rate, frame limiter, and V-sync state. Consumable captures additionally require the exact consumable, use/count expectation, starting HP, active damage-effect state, and passive-healing state. Requested action offsets are distinct from the request-relative UTC/stopwatch offsets of every audible signal, and CapFrameX API response latency is recorded; the analyzer uses the observed signal offsets. It also records that ordinary hydration/energy decay is accepted rather than pretending those continuously changing survival timers can be frozen. The wrapper fails before capture when any required material control is blank or a known placeholder. Earlier sidecars remain immutable evidence but cannot prove controls added after their creation.

## Configurations

| ID | Harmony | UDS | Required build |
| --- | --- | --- | --- |
| A | off | off | clean game control |
| B | on | off | dependency control and primary baseline |
| C | on | on | merged production v0.8.0 |
| D | on | on | final production v0.8.1, default diagnostics, all metrics enabled |

The wrapper requires no active mods for A, only `HarmonyLoadMod` for B, and exactly Harmony plus UDS for C/D. Production C must report `info.ini` version `0.8.0`; production D must report `0.8.1`. Build hashes remain authoritative because diagnostic builds deliberately do not masquerade as acceptance candidates.

## Matched controls

Use cold Duckov launches and the same save slot/generation, route state, location, facing, weapon instance, modifications, ammunition, magazine/shot count, graphics settings, display mode, resolution, refresh rate, frame limiter, V-sync, background-application state, duration, action timing, and collector flags. Exclude loading, menus, inventory management, route transitions, unrelated movement, and active injury/status changes. Natural hydration and energy decay is allowed: it occurs continuously, the user identified it as non-material for these stationary sequences, and the clean-A per-run medians remained stable across the decay. Keep cell order and elapsed timing reasonably consistent, but extract whenever needed to protect the retained weapon and character. Record any other condition that cannot be held constant before deciding whether a run remains valid.

Each acceptance cell needs at least three valid captures. Calculate each run independently and take the median of its run-level result. Keep invalid captures and record the objective invalidation reason; an inconvenient result is not an invalidation reason.

The standard 30-second firing capture has five stationary pre-action seconds, a frozen scenario-specific action window, and stationary recovery through capture end. Signals mark capture start, action start, action end, and capture end. The primary MP7 pilot uses one 30-round magazine and an action window from seconds 5 through 8: fire continuously at the action-start signal until empty, then remain still; the low signal closes the correlation window. The raw pilot determines whether that three-second window safely contains the full firing phase before acceptance timing is frozen. Acceptance captures use the same recorded shot/magazine sequence and action timing for every matched configuration.

After the matched D/B matrix is complete, one supplementary production-D soak records an entire natural run across at least three sequential map loads. This is deliberately not another matched acceptance cell and cannot replace or weaken the frozen D/B ceiling. On the installed CapFrameX 1.8.6 UI, the user sets capture time to `0` (unlimited), leaves sensor logging Off, starts and stops the record with one deliberate F11 press at each boundary, and retains the native JSON because that UI exposes no JSON+CSV option for manual capture. Codex does not launch or control Duckov. The untrimmed record includes route transitions and loading; the report separately names the actual raid interval, each native run segment/map interval, and equal early/middle/late raid slices by aligning CapFrameX's logged capture-start UTC with its raw relative clock and authoritative UDS UTC lifecycle timestamps. The JSON `Info.CreationDate` is completion-time metadata and is not misrepresented as capture start. Natural movement, combat, weapon swaps, survival decay, and any adverse result remain in evidence. The user reports perceived hitches only after stopping the capture so observation does not add mid-run focus stalls.

The soak-discovered Med-Kit correction uses a separate matched `consumable-medkit` cell. Run Harmony-only B before deploying the hardened D candidate, with one cold launch per configuration and three valid 30-second captures in the same safe Nullpunkt area. Begin exactly one Med-Kit (S) use at second 5, make no further input, and keep the action window open through second 15 so the completion/heal frame is isolated. Each run records starting HP, available Med-Kit count, absence of active enemies, damage-over-time effects, and passive healing, plus whether the use completed and a hitch was perceived. The healing totem present during the exploratory three-map soak is deliberately removed for both matched configurations: its independently timed healing would move the pre-action HP and could overlap the completion frame, while D but not B would also execute UDS health callbacks. All other equipment remains fixed. The first B capture is a pilot and freezes timing only after its raw completion frame is inspected. D must preserve exactly one item activation and its actual attributed healing per accepted use; the comparison reports the completion-window distribution and exact aligned peak, not only the whole-capture p99.

## Static audit before the pilot

The complete M1-M8 production trace established hypotheses to measure, not permission to optimize before the native control:

- M4's accepted firing callback constructs identities, display names, integrity state, event capabilities, and an equipment association, then reduces weapon and equipment statistics into both the active run and current route segment.
- M5 correlates every projectile at initialization and patches private `Projectile.Update`. Every live correlated projectile therefore enters and exits an ambient Harmony scope on every frame, including empty-space flight. The installed 2.3.30 assembly performs its raycast and calls `DamageReceiver.Hurt` inline in that private update; no narrower native hit event was found.
- Projectile completion emits accuracy evidence and performs the same run/segment combat fan-out. Every `Health.Hurt` prefix captures an equipment association before actual HP loss or target relevance is known; an actual transition then resolves identities/cause and reduces combat/equipment statistics into run and segment scopes.
- M6's equipment association provider synchronously rebuilds the complete equipment/totem snapshot. A build sorts slots, traverses and hashes attachments, scans tote contents, builds loadout/totem/snapshot identities, and publishes the observation. An unchanged observation still advances and clones current equipment state in both run and segment aggregates. Accepted firing plus projectile initialization can therefore request at least two complete equipment observations per projectile before any hit.
- M8 adds source/outcome segment association and doubles applicable weapon/combat/equipment reducers into current route-segment aggregates. Association recording also searches retained segments before appending bounded evidence.
- Dirty firing/combat mutations no longer write once per event. The scheduler permits a growing active-run clone, validation, JSON serialization, disk flush, and atomic replacement at a bounded one-second cadence, plus transition/terminal/shutdown safety flushes and bounded retry. This remains a possible periodic hitch rather than a per-shot write.
- The raid UI does no layout work while hidden; its per-frame tick only checks the hotkey. Combat Harmony patch-set reflection runs at a two-second conflict-check cadence. Ordinary firing/combat paths do not log per event.

History locates the cumulative cost windows without assigning blame: firing statistics entered in M4, projectile/update/health attribution in M5, equipment association in M6, and route fan-out in M8. External A/B/C controls and, only if needed, the opt-in counters decide which costs are material.

## Required scenarios

| Scenario ID | Required activity | Representative weapon |
| --- | --- | --- |
| `idle` | stationary raid gameplay, no input | same equipped loadout |
| `high-rate-empty` | fixed automatic sequence into open empty space | Electrified MP7 |
| `high-rate-single` | same weapon/sequence against one reproducible enemy | Electrified MP7 |
| `high-rate-multi` | same weapon against multiple reproducible targets | Electrified MP7 |
| `second-auto-empty` | fixed automatic sequence into open empty space | distinct available automatic frozen before its first paired capture |
| `second-auto-single` | same second automatic against one reproducible enemy | same frozen second automatic |
| `slower-control` | fixed slower-firing cross-class sequence | Mosin-Nagant (`#781`) |

The Vektor used in the historical post-M8 stress observation was lost during that run and is unavailable for the controlled matrix; no save or inventory is modified to recreate it. The retained Electrified MP7 becomes the primary high-rate control. The final evidence must still include a distinct available automatic and a slower cross-class control, and must not be overfit to the MP7. A scenario may be declared unreproducible only with a concrete reason; it may not disappear because its result is unfavorable.

The frozen primary pilot uses the retained 97/97-durability Electrified MP7 with Rost-Muni S. The initial loadout declaration recorded 30 rounds loaded and 286 in inventory; after the discarded collector attempts and before the first accepted clean-A launch, the user authoritatively reported 30 loaded and 320 remaining. Each capture records its actual start count rather than projecting from the earlier declaration. The weapon's optic, MP muzzle, and vertical-grip slots are occupied; its tactical and magazine-modification slots are empty. The localized optic and muzzle labels are visually truncated, so the authoritative control is the unchanged retained weapon instance plus the slot-state screenshot rather than an invented full name. Display controls are 2560×1440 borderless fullscreen at 240 Hz, a 240 FPS limit, V-sync off, FSR 1.0 off, and every remaining graphics option at its highest available High/On value. The same weapon instance, attachments, ammunition, location, facing, and firing sequence must be retained across each matched configuration; reserve ammunition may decrease between valid cells but the capture begins with the same 30-round loaded sequence.

Before the first cross-weapon paired capture, inventory screenshots freeze the distinct automatic as the `MF` (`#242`), a conventional AR-class weapon with 13.5 fire rate and a 30-round magazine, and the slower cross-class control as the `Mosin-Nagant` (`#781`), a sniper-class weapon with 0.5 fire rate and a five-round magazine. Both retained instances start at 100/100 durability with no attachments, and the user confirmed sufficient ammunition for the complete paired matrix. The MF uses one continuous complete 30-round magazine inside the existing 5-8-second action window for both empty-space and single-enemy scenarios. The Mosin uses one complete five-round magazine, firing each shot as soon as the weapon permits, inside a 5-16-second slower-control action window. The exact selected ammunition names and loaded/reserve counts are recorded when each weapon is first loaded and must then remain matched through B and D. Actual wear and reserve depletion are recorded per run; neither weapon, its attachment state, its ammunition type, nor its sequence may be substituted mid-matrix.

## Metrics and acceptance

For every capture report duration, frame count, average FPS, mean, median, p95, p99, maximum frame time, counts and percentages above 8.33/16.7/33.3 ms, every >33.3 ms offset, whether the spike occurred during the action window, dominant/non-dominant swap-chain rows, and warnings. For action scenarios, independently report the frame count, summed frame duration, average FPS, median, p95, p99, maximum, and threshold percentages for only the frozen action window; whole-capture medians must not dilute the firing phase.

Primary comparison is D versus B. Secondary comparisons are D/A, D/C, and B/A.

- Engineering target: D/B median frame-time overhead at most 5%, p99 overhead at most 10%, indistinguishable >16.7 ms rate, and no UDS-specific >33.3 ms frame or cluster.
- Maximum ceiling: D/B median overhead at most 10%, p99 overhead at most 20%, no repeatable increase above 16.7 ms greater than 0.25 percentage points unless raw evidence proves unrelated noise, and no repeatable new >33.3 ms cluster.
- Cluster: at least two >33.3 ms frames within a rolling one-second window during the same practical firing phase, present in at least two of three UDS-enabled captures and absent from corresponding controls.

A single isolated maximum outlier is diagnostic rather than an automatic failure. One repeatable weapon/scenario failure rejects the candidate even if aggregate averages pass.

## Opt-in diagnostics

Ordinary A/B/C/D acceptance uses production builds. If raw controls prove a UDS delta but cannot isolate it, build the bounded counter variant with:

```powershell
.\scripts\build-performance-diagnostic.ps1 -DuckovPath 'E:\SteamLibrary\steamapps\common\Escape from Duckov'
```

It creates an ignored exact-five-file diagnostic package and manifest without deploying it. Compile-time-only counters distinguish accepted firing callbacks, tracker mutations, projectile initialization/update/release volume, health prefixes/transitions, equipment association requests and snapshot builds, reflective Harmony patch-set inspections, and checkpoint clone/store frequency. They emit one summary at shutdown, perform no per-event logging, and have zero call sites in ordinary production builds. Deployment still requires immediate confirmation while Duckov is closed.

## Measured diagnosis and hardening

The accepted A/B/C controls isolate a CPU-side UDS regression. Harmony alone remains indistinguishable from the clean control, while production v0.8.0 fails the matched B ceiling at idle p99 and at high-rate empty-space firing p99 and material-frame rate. The opt-in counter build then established exact production-boundary frequency rather than relying on source shape alone:

- A 30-second idle interval performed 150 complete equipment builds, 260 reflective patch-set inspections, and six full checkpoint clone/store operations despite no gameplay input.
- One matched 30-round empty-space interval performed 30 firing reductions, 30 projectile captures/completions, 348 per-frame projectile scopes, 60 equipment-association requests, 213 complete equipment builds, 272 patch-set inspections, and eight checkpoint clone/store operations.
- No health transition occurred in the empty-space interval. The firing interval therefore isolates hot work before enemy identity, damage, kill, or hit fan-out is added.

The v0.8.1 correction keeps every accepted event and attribution but changes where repeated work occurs:

- Public slot, hold, inventory, and item-tree events immediately refresh an immutable equipment snapshot. Firing and projectile associations read that event-time cache instead of rebuilding and republishing the entire attachment/totem tree. A one-second watchdog reconciles missed external mutations without defining normal event timing. Unchanged snapshots are not republished within the same segment, but the same immutable loadout is published once after every segment change so segment-local equipment roots remain complete.
- Healing, combat, and container patch sets still receive full ownership validation at activation. UDS then captures the reference identity of Harmony 2.4.1's serialized shared-state token for each original method. Every subsequent patch or unpatch replaces that token; runtime integrity checks therefore require only a locked dictionary lookup and reference comparison, distributed across the existing two-second cycle. Healing effect/health/buff callbacks perform the same stamp check at the callback boundary. A changed, missing, or inaccessible token conservatively disables the dependent capability; no runtime check deserializes patch metadata, launches background reflection, or weakens conflict detection.
- Projectile generation/run/map context is synchronized once per Unity frame while correlations exist. Ambient scopes retain direct reference identity and health processing reuses the application-time association already carried by the active scope.
- The main thread still creates the exact immutable active-run checkpoint at the existing one-second dirty/five-second periodic cadence. Validation, JSON serialization, durable flush, and atomic replacement execute as one single-flight background write; lifecycle, profile, transition, terminal, export, and shutdown boundaries wait for it. A captured mutation revision prevents an older completion from clearing newer dirty state, and failed writes remain visible and retryable.

Deterministic tests prove cached association identity across 60 reads, immediate event invalidation, the watchdog bound, complete non-burst stamp coverage, patch-state change/missing-state/owner rejection, zero managed allocations across 10,000 trusted stamp checks, nested reference-scope unwinding, single-flight checkpoint submission, exact-value persistence, failure surfacing/retry, non-consuming waits, and post-snapshot dirty-state preservation. Final acceptance still uses configuration D with ordinary production diagnostics and the complete feature set; none of these structural proofs substitutes for the matched native matrix.

The complete frozen three-run matrix passes the maximum release ceiling in every accepted D/B cell and introduces no repeatable severe cluster. Whole-capture median/p99 overhead is +0.568%/+7.612% at idle, +0.674%/+11.136% for MP7 empty-space firing, -1.259%/+2.065% for MP7 single-enemy firing, +0.839%/+6.792% for MF empty-space firing, -7.514%/-3.775% for MF single-enemy firing, and +0.924%/+7.388% for the Mosin control. Action-window overhead is +2.217%/+12.506%, -5.061%/+5.747%, +0.945%/+3.102%, -11.768%/-1.611%, and +1.057%/+8.816%, respectively. MP7 empty-space firing is therefore acceptable for v0.8.1 under the 10% median/20% p99 hard ceiling but misses the tighter 10% p99 engineering target; all other accepted cells meet the tighter target.

The required secondary comparisons preserve that interpretation. Harmony-only B versus clean A is -0.315%/-2.557% whole-capture median/p99 and -1.555%/-5.422% in the MP7 empty-space action window, with a -0.014 percentage-point whole-capture change above 16.7 ms and zero action-window change; idle is -0.017%/-0.637% with zero material-frame-rate change. Corrected D versus A is +0.358%/+8.294% whole and +0.628%/+6.406% in the firing window, with -0.014/+0.000 percentage-point changes above 16.7 ms; idle is +0.551%/+6.926% with zero change above 16.7 ms. Corrected D versus merged v0.8.0 C is +0.039%/-18.145% whole and -0.491%/-35.045% in the firing window, improving the >16.7 ms rate by 0.115 and 0.466 percentage points; idle is +0.328%/-17.177% with a 0.087-point improvement. None introduces a repeatable cluster. The exact reports are `artifacts/performance/analysis/final-production-matrix.json` (119,531 bytes, SHA-256 `e9039e2b99eade5228b92b6f20eb8e3a7e925accfdcbe2c08266af2b53d1cc8c`), `final-production-matrix-baseline-a.json` (56,969 bytes, `d688c9d4cfd0e6d5020183b95706e9a957f4e875dc06f9e74a86617071c5c604`), and `final-production-matrix-baseline-c.json` (57,040 bytes, `a5d55eea2f30efc4201bf431f0e45b40180ec35ed3bf6aba09008b3c4cf865ee`).

### Supplementary three-map soak and consumable completion

The manual production-D soak retained 141,955 raw frames across 710.527 seconds and the complete extracted route Nullpunkt → Lagerbereich → Farmstadt. The actual raid window has 139,455 frames, a 4.571 ms median, 9.040 ms p99, and 23 frames above 33.3 ms. Equal early/middle/late raid slices measure 10.391/9.464/8.650 ms p99, so tail latency did not worsen as the run or route grew. The two loading transitions remain separately named and are not treated as gameplay. The untrimmed report is `artifacts/performance/analysis/candidate4-full-run-soak-detailed.json`; the raw native JSON is `artifacts/performance/captures/candidate4/D/full-run-soak/r01/d-full-run-soak-r01.capframex.json`.

The soak exposed a different repeatable hot path outside the frozen firing matrix. The user reported a very noticeable hitch on every one of eight Med-Kit (S) completions and a slight hitch on the one hydration-consumable completion. Persisted segment-event UTCs align all eight Med-Kit completions with CPU-side frames between 126.12 and 735.90 ms; the single potato and Limonade completions each align with an approximately 69 ms CPU-side frame. GPU busy remains only 2.49-4.45 ms for those peaks. Exact timestamps, offsets, per-event CPU/GPU values, severe-frame counts, and the 8/8 user confirmation are preserved in `artifacts/performance/analysis/candidate4-full-run-soak-consumable-correlation.json`. This supplementary D-only evidence establishes repeatability and source correlation but does not by itself separate base-game work from incremental UDS cost.

Production tracing explains the measured multiplier: every accepted consumable completion synchronously serialized, flushed, and atomically replaced the approximately 1.24 MB lifetime profile on Duckov's main callback, then synchronously persisted a success diagnostic. A Med-Kit also emits attributed healing in the same completion path, causing a second full profile replacement and a second success diagnostic. Firing and combat do not update lifetime profile aggregates per event, so the earlier firing matrix could not expose this separate M1/M2 path.

The corrective candidate keeps both lifetime and active-run reductions synchronous and exact at event time, but marks the lifetime profile dirty and captures one immutable frame-level snapshot after item-use and healing callbacks have coalesced. Only the small mutable lifetime-item aggregate, identity, capabilities, and collection roots are cloned on the main thread; serialization, durable flush, and atomic replacement run through a single-flight background writer. Run completion, transitions, save collection, slot/generation changes, UI export, clean shutdown, and disposal drain the latest snapshot with one bounded retry. Successful events no longer rewrite the separate diagnostic document; failures remain logged and durable.

Crash safety is not delegated to timing luck. An optional persisted per-run lifetime watermark records the exact item aggregate already present in each profile snapshot. Interrupted-run recovery compares it with the newer active-run checkpoint, validates that it is a component-wise and compositionally consistent prefix, and applies only the missing lifetime delta before clearing the watermark. Profiles from immediate-save builds have no watermark and retain their prior recovery behavior. Deterministic coverage exercises same-frame coalescing, mutation during a pending write, a fresh bounded retry, immutable snapshot isolation, no-snapshot crash recovery, partial-prefix crash recovery, group promotion, legacy recovery, exact-once restart, and rejection of malformed watermark roots.

The corrected Harmony-only B cell establishes the native Med-Kit reference rather than assuming completion is stall-free. Its three valid uses each restored exactly 12 HP with the healing totem unequipped and no enemy, damage effect, passive healing, unrelated input, or perceived hitch. Median-of-runs whole-record median/p99 is 4.201/5.026 ms and the observed completion-window result is 4.216/5.100 ms. Candidate 7's three matched enabled uses preserve exactly three activations, 75 durability consumed, and 36 actual HP restored. Their whole-record median/p99 is 4.432/5.512 ms and their action-window median/p99 is 4.464/5.671 ms: +5.498%/+9.677% and +5.868%/+11.191% over B. Exact completion-window maxima fall from the original soak's 126.12-735.90 ms CPU stalls to 6.157-17.516 ms, none is perceived, and every measure passes the 10% median/20% p99 hard ceiling. Isolated delayed CPU frames remain visible in two B and two D captures, but they form no cluster and do not align with completion; the shared timing shape does not support attributing them to UDS from this sample.

Candidate 8 adds the segment-aware equipment publication correction after a pre-soak audit proved that a cache keyed only by loadout identity could leave a new segment without its local equipment root. Its immutable commit `fb61cd86c4a31ed786f50e87bb8d6c90bba5e94d` passes all 505 Release tests and exact package/deployment readback. The first candidate-8 natural run ended in death on Lagerbereich before Farmstadt, so it is retained as partial evidence and excluded from the frozen full-route gate. Across its 82,661 raid frames, the reached Nullpunkt and Lagerbereich portions measure 4.243 ms median and 7.819 ms p99; late-third p99 is 12.575% better than early-third p99. Three Med-Kit completions peak at 13.080, 14.743, and 8.299 ms in their exact windows, and the two food/hydration completions peak at 10.305 and 7.386 ms. The user perceived no gameplay or completion hitch. The unchanged MP7/MF loadout is present in both persisted segments, directly validating the segment-aware cache correction. Because the run contains only one of the two required transitions, another candidate-8 run reaching Farmstadt remains required before the representative full-route gate closes.

The candidate-8 retry reaches the complete Nullpunkt → Lagerbereich → Farmstadt route and ends in a normally finalized eligible death. Its 93,749-frame raid interval measures 4.470 ms median, 10.532 ms p99, and 0.076% of frames above 33.3 ms. The equal early/middle/late p99 values are 11.479, 6.887, and 11.601 ms; late p99 is only 1.059% above early p99 and therefore remains far inside the predeclared 20% elapsed-run stopping rule. Post-load gameplay p99 is 8.459 ms in Nullpunkt, 6.893 ms in Lagerbereich, and 9.607 ms in Farmstadt. The user perceived no UDS-related gameplay hitch and reported that all three Med-Kit completions and the hydration completion felt smooth. The only perceived stall followed Farmstadt loading and was identified as the game's normal map-load hitch, which also occurs without UDS.

The persisted run contains exactly 306 firing actions, 306 completed projectiles, 200 hits, 25 kills, five item uses, 60.0000038 HP restored, three non-empty segment equipment roots, and 891 distinct source/outcome associations with no duplicate identity. The five completion windows correspond to three Med-Kits plus one Teigtasche and one Flaschenwasser; their non-healing order is not claimed because the user does not remember it. Exact-window maxima are 14.843, 470.821, 12.982, 10.358, and 37.008 ms, but repository formatting and documentation writes overlapped this natural capture. That Codex-side background activity—not user input—makes isolated maxima and mechanically detected raw clusters unsuitable for UDS causality. The full-route soak remains valid for persistence correctness, perceived smoothness, and elapsed-run p99 behavior, while the clean matched D/B matrix remains the acceptance source. The UI and twenty-file export agree with the live profile and backup; normal shutdown removes all live residue and leaves byte-identical 1,089,636-byte primary/backup profiles. The raw JSON is 10,457,341 bytes at SHA-256 `573c4e5a6daec2a0ab2ac3be344487150da206d0d47b20eeb8ce589b79a43eb4`; the detailed report is `artifacts/performance/analysis/candidate8-post-hardening-full-run-soak-r02-detailed.json` (79,810 bytes, `f132634407ecac0a8d0c99073d253662ad35dd53748ad43bb7fdcaddc992cad9`), and the complete qualification is preserved in `d-post-hardening-full-run-soak-r02.evidence.json` (11,903 bytes, `715d3fb6361fbb5ee922ad56a4403858f2f220c6b69c44d81c77ab98bcea8898`). No additional natural soak or speculative hot-path rewrite is justified by this evidence.

The accepted immutable production artifact is built from commit `c2276aa4fc6629c4e71bb0302401ff1abc267792`. `UltimateDuckovStatistics-v0.8.1.zip` is 221,742 bytes at SHA-256 `853f26cec49048e68554e6a2e7a3804b6aa1b9dfbbe5a91072821bcb5433563a`; its exact five-file source package, 102-byte checksum sidecar, independent extraction, transactional installed-byte readback, cold-start capability readback, and clean shutdown all pass. Both DLLs report file version `0.8.1.0` and product version `0.8.1+c2276aa4fc6629c4e71bb0302401ff1abc267792`.
